# Completed Backlog Items

Backlog items closed out by `/archive-task` once their feature has merged. Moved here (out of `docs/backlog.md`) so the active backlog only shows what's still open.

- [x] Enforce conventional commits — done, see `docs/specs-archive/202607251245-enforce-conventional-commits/`
- [x] Refactor to use the Result-pattern error/logging standards — migrated `token-core`'s `parseTokenFile`/`parseNode` and the `web-app` consumers off thrown errors onto `neverthrow` `Result`s, and stood up `@dtcg-editor/errors` (`UnknownError`, injected `Logger`) — done, see `docs/specs-archive/202607251455-migrate-to-result-pattern-error-handling/`
- [x] Bootstrap CI (GitHub Actions) — build/lint/test checks on PRs and pushes to `main` — done, see `docs/specs-archive/202607251627-bootstrap-ci-github-actions/`
- [x] Migrate `apps/web-app/lib/config.ts`'s `ConfigError`/`loadConfig` to the Result pattern — separate call path (startup config loading via `instrumentation.ts`) — done, see `docs/specs-archive/202607280011-migrate-config-result-pattern/`
- [x] CI-level Conventional Commit enforcement — lint actual commit messages/PR commit range in CI (not just the commitlint config's own unit tests); local husky `commit-msg` hook remains the only enforcement today. Deferred from the Bootstrap CI feature — done, see `docs/specs-archive/202607280019-ci-conventional-commit-enforcement/`
- [x] CLI to bootstrap `dtcg-editor.config.json` — interactive/flag-driven command that scaffolds the web app's config file (currently just `tokensDir`) for a new user instead of hand-writing JSON; validates against the same schema `instrumentation.ts` uses at startup — done, see `docs/specs-archive/202607280827-cli-bootstrap-config/`
- [x] Define UI-layer `Result` consumption conventions (React hooks, error boundaries) once real client-side component code exists — `docs/project.md`'s Error Handling constraint explicitly defers this; also covers giving `FileNotFoundError` its own distinct message in `page.tsx` instead of the generic fallback — done, see `docs/specs-archive/202607280829-ui-result-consumption-conventions/`
- [x] Fix Edge Runtime warning for `process.exit` in `apps/web-app/instrumentation.ts` — isolated the Node-only fatal-startup-exit path behind a dynamic import of a new `apps/web-app/lib/fatal-startup-error.ts` module, mirroring the existing `config.ts` dynamic-import pattern, so Turbopack's Edge Runtime static analysis no longer flags `process.exit` — done, see `docs/specs-archive/202607280933-instrumentation-edge-runtime-process-exit/`
- [x] Inject dependencies by default (e.g. `fs.readFile`, etc.) — establish a convention for passing I/O/platform calls in as explicit parameters rather than importing and calling them directly, for testability and consistency; interacts with the Result-pattern refactor's Logger-injection approach — done, see `docs/specs-archive/202607281409-inject-dependencies-by-default/`
- [x] Reformat the repo to use tabs instead of spaces — added Prettier (`useTabs: true`), `.editorconfig`, `.prettierignore`, reformatted 147 existing files repo-wide with zero behavioral drift, and wired `pnpm format:check` into CI — done, see `docs/specs-archive/202607281440-reformat-repo-to-tabs/`
- [x] Improve UI. Section/group names should be editable. There should be a label for all fields. — non-root groups are now renameable through the same batched edit/save pipeline tokens use (with cascading descendant path updates and a batch-ordering fix in `token-core`), and every field (editable and read-only) now has a real visible label instead of relying solely on `aria-label` — done, see `docs/specs-archive/202607281844-editable-group-names-and-visible-field-labels/`
- [x] Save button should look like a larger nicer call-to-action button with a disk icon. Make it its own component — extracted into `apps/web-app/components/SaveButton.tsx`, restyled as a larger rounded solid-accent CTA with hover/focus-visible/disabled states and a hand-rolled inline disk-icon SVG; new `--accent`/`--accent-hover`/`--accent-foreground` CSS variables added to `globals.css` — done, see `docs/specs-archive/202607282347-save-button-as-a-call-to-action-component/`
- [x] Components like DimensionEditor need to be enabled via config, not direct imports. The ones defined in this repo will be default values in the config — replaced with a committed `dtcg-editor.config.mts` calling a new `defineConfig({ tokensDir, extensions })` helper; `extensions` lets a user override which component edits a subset of tokens, merged with built-in defaults — done, see `docs/specs-archive/202607292258-config-driven-token-editor-components/`
- [x] Prettier should be run in a pre-commit step to ensure formatting is applied — done, see `docs/specs-archive/202607300736-prettier-pre-commit-formatting/`
- [x] Accessibility testing — two-tier automated WCAG 2.2 AA checking (Vitest Browser Mode component tests + Playwright page/keyboard-flow tests) — done, see `docs/specs-archive/202608011523-accessibility-testing/`
- [x] Generic fallback token editor: provide a default/fallback interactive editor for token kinds that have no registered editor (currently read-only only) — should also be used to more thoroughly test the config-driven editor-extension mechanism's genericity (e.g. via a second, non-default editor), a test depth intentionally deferred from the DimensionEditor config-enablement feature — done, see `docs/specs-archive/202608012104-generic-fallback-token-editor/`
- [x] Support for colour tokens — new `@dtcg-editor/token-type-color` package (all 14 DTCG 2025.10 colorSpaces + legacy bare-hex), CSS-native swatch preview, non-blocking per-colorSpace range validation, and a full `ColorEditor` UI; a follow-up fix generalized `canEdit`/the PATCH handler beyond dimension so any built-in type's value is schema-validated client- and server-side before being editable/accepted — done, see `docs/specs-archive/202608021545-support-for-colour-tokens/`
- [x] Allow the runner to specify in config which colour spaces are available, and add an interactive colour picker for colour tokens (merged from two separate colour-related items) — new generic `editorOptions`/`editorOptionsSchema` config channel lets a host app restrict `ColorEditor`'s offered colorSpaces via `defineColorConfig`, plus a native `<input type="color">` picker (backed by `colorjs.io`) wired into both the object and legacy-hex color editors — done, see `docs/specs-archive/202608022222-configurable-color-spaces-interactive-color-picker/`
- [x] Migrate ESLint + Prettier to Biome — prerequisite for the TypeScript v7 upgrade; replaced ESLint (`typescript-eslint`, `eslint-config-next`) and Prettier repo-wide with Biome 2.5.8, reproducing all 11 Dependency Injection for I/O/Platform Externalities rules 1:1 (native rules + 7 hand-authored GritQL plugins), removing `typescript-eslint`'s `typescript<6.1.0` peer-range blocker for the still-open v7 upgrade item below — done, see `docs/specs-archive/202608161600-migrate-eslint-prettier-to-biome/`
- [x] Remove legacy sdd-* and project.md files. Migrate all docs into speckit — no dedicated feature spec (not run through the speckit pipeline; done directly): sdd-* skills deleted from `.agents/skills`/`.claude/skills` (commit `cf03fd7`, principle-level content already migrated into `.specify/memory/constitution.md` in commit `12cff73`). Completed the remaining half: `docs/project.md` removed; its running log (Features, Architecture Decisions, API, Environment & Configuration — content the speckit constitution has no place for) moved to `docs/history.md`; `archive-task`, `CONTRIBUTING.md`, and `docs/backlog.md`'s header updated to stop referencing `docs/project.md`/`/sdd-feature`.
- [x] Refactor token-type subpackages: move all parsing and type definitions into `token-core`, so the `token-type-*` subpackages hold only editor UI — moved every DTCG type's structural value schema (`ColorValueSchema`, `DimensionValueSchema`, derived types) into `token-core`, and renamed `token-type-color`/`token-type-dimension`/`token-type-contract` to `token-editor-color`/`token-editor-dimension`/`token-editor-contract`, trimmed to hold only `Editor` UI, editor config, contract wiring, and a new `src/utils/` subfolder for value-adjacent logic that isn't structural parsing (range validation, native-widget conversion, CSS rendering) — done, see `docs/specs-archive/202608162249-token-core-parsing-consolidation-and-token-editor-rename/`
- [x] Refactor web-app tests to use mock token files instead of referencing internal token files that can change at any time and break tests; the web-app is generic and loads any token files supplied via config, so tests should reflect that. Also audit for other similar cases (tests coupled to internal/example data rather than fixtures) and address them in the same pass — no dedicated feature spec (not run through the speckit pipeline; picked up directly via `pick-up-task`): `e2e/tokens-page.spec.ts` and `e2e/keyboard-navigation.spec.ts` were hardcoded to the stale, unserved `sample_data/` directory; repointed at a new e2e-owned `apps/web-app/e2e/fixtures/tokens/` directory served via a new `DTCG_EDITOR_TOKENS_DIR` env override, plus fixed drifted `keyboard-navigation.spec.ts` selectors. All other tests were already isolated with inline mocks/fixtures. Filed a follow-up backlog item for a pre-existing, unrelated WCAG contrast bug the fix exposed.
- [x] Add linting rules enforcing React component filename/folder conventions: component filenames must be PascalCase, and each component must live in its own folder alongside its co-located tests, styles (CSS modules), and other component-scoped files — enforced via `@ls-lint/ls-lint` under `pnpm lint`; scope grew during implementation to cover every `packages/*/src/components` (not just `apps/web-app/components`), migrating `packages/design-system` (denested from `ui/`), `packages/token-editor-color`, and `packages/token-editor-dimension` to comply — done, see `docs/specs-archive/202608190702-react-component-file-folder-linting/`
- [x] Refactor all CommonJS (`.cjs`, `require`/`module.exports`) files in the repo to ES modules, and add a constitution requirement to default to modern module syntax (ESM) over CommonJS where possible — done, see `specs/004-cjs-to-esm-migration/`
- [x] Add a light/dark mode switcher following the two-state toggle approach from Lea Verou's "Dark mode toggles: theory and practice" — done, see `specs/006-light-dark-toggle/`
- [x] Improve token references: (1) a reference to another token should show the value it resolves to, inline; (2) the reference name should be a link that navigates to the referenced token so it can be edited directly; (3) a token that other tokens reference should display a "referenced {once,twice,N times}" indicator, expandable into a dropdown list of every token that references it, each a link back to it. Consolidates three prior separate backlog items (preview the referenced value; hotlink to the referenced item; view/jump to referencing items) — done, see `specs/007-token-reference-links/`
- [x] TreeGroupNode should be refactored to either be a disclosure element, or make sure it has all necessary aria props like controls, and expanded — absorbed into the token-references feature: converted to a native `<details>`/`<summary>` disclosure, which also gave arrival its collapsed-group reveal for free (per its own architecture, see `docs/history.md`) — done, see `specs/007-token-reference-links/`
- [x] Refactor `TreeTokenNode` to extract a reusable "dumb" CUBE-CSS Block subcomponent (deduplicating existing repetition) and move the relevant styles out of `TokenTree.module.css` into it; stop repeating the token name in every label ("Name" not "{TOKEN} name"); promote the token name to a semantically-correct heading; refactor `Badge` into a "pill" style for the type label; give each token node a type-based icon; add a left pin line per token with a visible break between sequential tokens — extracted the presentational `TokenBlock` component (its own CSS module, out of `TokenTree.module.css`), name now an `<h2>`, type as `Badge` pill, inline-SVG type icons with a fallback, per-token pin line. Done, see `specs/005-tree-token-node-block/`
- [x] Make token editing fast and seamless: an edit should apply instantly with no perceptible lag, and keyboard navigation — tabbing between fields, moving focus around the page — must not trigger unexpected layout reflows or component re-renders. Audit the editor render path for avoidable re-renders on focus/blur/tab and for layout shift when fields or previews mount/unmount or when values change — one React-free `StagedEditsStore` per `TokenTree` mount + `memo`'d rows + local per-field draft state; validation-message slots reserve space; `<details>` stays uncontrolled; the reference ripple is scoped to `key ∪ reverseDeps(key)`. Playwright perf/stability guards on a 2,000-token fixture with a recorded baseline. Done, see `specs/010-fast-seamless-editing/`
- [x] Add the ability to edit a token's reference (alias) value: a combobox/searchable-filterable picker over every token in the document that a reference could point at, showing a live preview of each candidate's resolved value — and the resulting value of the token being edited — as the user types, so a reference can be repointed without hand-authoring `{group.token}` paths — a new generic `Combobox<T>` design-system primitive (`cmdk` + Radix Popover) with a repaired `Command`; circular targets (including the token's own path) are unselectable, missing/group targets flagged but stay selectable; the idle popover shows only the current target (full list reachable by typing) and the rendered result list is capped at 200, both added mid-implementation to close a real SC-004 perf regression at ~2,000 candidates rather than virtualizing under `cmdk` (see `docs/history.md` Architecture Decisions). Done, see `specs/009-edit-token-references/`
- [x] `ColorEditor`'s value formats should render as familiar CSS color syntax (e.g. `rgb(...)`, `hsl(...)`, `oklch(...)`, `#hex`) matching each colorSpace, instead of showing the raw DTCG JSON component object — turned out to be about the read-only `ColorPreview` (used when previewing a reference's resolved value), not the interactive `ColorEditor`, which stays unchanged; now renders CSS Color 4 syntax reusing the same formatting already used for its swatch. Done, see `packages/token-editor-color/specs/002-color-preview-css-format/`
- [x] Add support for "duration" tokens — dedicated `DurationEditor`/`DurationPreview` in a new `@dtcg-editor/token-editor-duration` package (mirroring `token-editor-dimension`/`token-editor-color`), `DurationValueSchema` in `token-core`, registered as a built-in type — done, see `specs/011-duration-token-support/`
- [x] Add support for "cubicBezier" tokens — done, see `specs/011-cubicbezier-token-support/`
- [x] Add support for "fontWeight" tokens — adds `token-core`'s `FontWeightValueSchema` (DTCG 2025.10-conformant integer 1-1000 or one of 18 keyword aliases) and a new `@dtcg-editor/token-editor-font-weight` package (numeric `FontWeightEditor` + optional keyword-alias picker, `FontWeightPreview` mirroring `ColorPreview`'s pattern, full `TokenTypeContract` wiring), registered alongside `dimension`/`color` in `apps/web-app/lib/token-editors/built-in.ts` — done, see `specs/011-font-weight-token-support/`
- [x] Add a `DimensionPreview` to `packages/token-editor-dimension` so `dimension` — the last currently-registered token type still missing one — stops rendering raw JSON in `TokenReferenceValue`/`CandidatePreview`, mirroring `ColorPreview`/`DurationPreview`/`CubicBezierPreview`/`FontWeightPreview`'s validate-then-render pattern; also generalized the package's build script to copy every `*.module.css` under `src` (matching `token-editor-duration`'s already-generalized version) instead of one hardcoded `cp` per component — done, no dedicated `specs/` directory (small enough to drive directly with TDD, not run through the full spec/plan/tasks pipeline)
- [x] Made `Preview` a required field on `TokenTypeContract` (`packages/token-editor-contract`) now that every registered contract has one, so a future token type can't silently omit it and regress to the JSON fallback — a `@ts-expect-error` fixture in `contract.test.ts` proves the omission fails to type-check, replacing the prior test that asserted the opposite — done, no dedicated `specs/` directory (small enough to drive directly with TDD)
