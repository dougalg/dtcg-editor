# Project: dtcg-editor

## Mission

dtcg-editor is an open source editor for DTCG (Design Tokens Community Group) design token files, built as a monorepo with two audiences: tool builders and integrators, who embed the core engine and token-type packages into their own design tooling; and design system teams, who use the bundled web app as a ready-made editor for their DTCG token files with minimal configuration.

## Tech Stack

- Language: TypeScript
- Framework: React (used in the UI/token-type packages and the web app; the core engine itself is intended to be an installable module rather than a full web app)
- Package management: pnpm workspaces
- Build orchestration: Turborepo
- Database: none
- ORM: none
- Migrations: none
- Messaging: none
- Testing: Node's built-in test runner (`node:test` + `node:assert/strict`) for `packages/*` (`token-core`, `errors`, `token-type-contract`, `token-type-dimension`) — none of these render JSX. `apps/web-app` uses Vitest + `@testing-library/react` (`jsdom` environment) instead, since `node:test`'s native TypeScript support only strips type syntax and cannot execute `.tsx`/JSX at all (confirmed empirically) — the "concrete gap" this line's own carve-out anticipated. See the Edit Dimension Tokens in Browser feature. `apps/web-app`'s tests touching `fs`/`fetch`/other injected I/O-platform dependencies use hand-rolled mocks passed through the injected parameter, not a real temp directory or real network call — see the Dependency Injection for I/O/Platform Externalities constraint below. Automated accessibility testing is two-tier: a second Vitest project (`test.projects`, name `a11y`) renders components in a real Chromium instance via `@vitest/browser`'s `@vitest/browser-playwright` provider and asserts zero `axe-core` WCAG 2.2 AA violations (co-located `*.a11y.test.tsx` files, alongside the unchanged `unit`/jsdom project); a `@playwright/test` suite in `apps/web-app/e2e/` separately checks whole-page axe results and keyboard-only navigation/focus order against the real running app. Both are enforced (non-warn-only) under `pnpm test`/`turbo run test`. See the Accessibility Testing feature.
- Other: not yet decided

## Architecture

Feature-based modules, organised as a monorepo with three kinds of packages:

1. **Core engine** — reads and renders a UI for editing DTCG token files. This is not a full web app; it is an installable module intended to be wrapped and delivered in different ways for different host applications.
2. **Token-type packages** — subpackages implementing the UI/validation/etc. layer for specific DTCG token types (e.g. `color`, `dimension`). Each token type is its own module/package rather than being handled by shared generic code.
3. **Web app** — composes the core engine and token-type packages into a UI for editing a set of DTCG files, requiring minimal configuration from the user.

Within each package, organise code by feature/domain rather than by technical layer (e.g. a `color` token package owns its own components, validation, and logic together, rather than being split across shared `components/`, `services/`, `validators/` directories). This mirrors the plugin/microkernel shape of the Token-Type Package Contract below: the code-organization boundary matches the plugin boundary, so adding or removing a token type stays a single-directory operation instead of requiring edits across several shared directories (see `docs/research/feature-vs-layer-organization.md` for the supporting architecture literature).

## Conventions

- **DTCG spec compliance is mandatory.** Token schemas, formats, and validation logic must strictly conform to the Design Tokens Community Group specification. Any deviation from the spec must be flagged explicitly rather than silently implemented. Interoperability with other DTCG tooling (Figma plugins, Style Dictionary, other editors) is this project's entire value proposition, so a silent spec deviation is silent data corruption in files the user owns — not a cosmetic bug.
- **Tests live alongside the code they test.** A `*.test.ts` file sits in the same directory as the module it covers (e.g. `parse.ts` + `parse.test.ts`, `scan.ts` + `scan.test.ts`), not in a separate `test/` directory. `node --test` (no args) discovers all of them recursively from each package's root. This follows directly from the feature/domain organization principle in Architecture above: a test is part of the feature bundle it verifies, and a separate `test/` tree would reintroduce the technical-layer split that principle exists to avoid.
- **Internal relative imports use an explicit source extension (`.ts`/`.tsx`), not extensionless or `.js`.** This lets `node --test` run test files directly against TypeScript source with zero build step (Node strips types natively), while `tsc`'s `rewriteRelativeImportExtensions` (paired with `allowImportingTsExtensions`) still rewrites these to `.js` automatically in any package's compiled `dist/` output for consumers. Applies uniformly to plain library packages (`token-core`) and the Next.js app (`web-app`) alike, since Turbopack also resolves `.ts`-extension specifiers correctly.
- Package naming, REST base path, and authentication conventions: not yet established — no code exists yet. Update this section once the first packages are scaffolded.

## Architectural Constraints

### TypeScript Strictness

All packages in the monorepo must compile under maximally strict TypeScript settings, with no per-package relaxation. This applies uniformly whether a package is publishable (core engine, token-type packages) or internal (web app), since the whole point of the strict-at-the-edges validation model only holds if the type system itself can't be silently defeated.

- Root `tsconfig.base.json` enables `strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `noImplicitOverride`, `noImplicitReturns`; every package `extends` it and may not loosen any flag.
- `any` is banned (lint-enforced); use `unknown` and narrow. Non-null assertions (`!`) require an inline comment justifying safety.

### Validation at the Edges

Validation happens once, at the true edges of the system — where data enters from outside the monorepo's control: file reads, pasted/uploaded JSON, host-app configuration, or a third-party consumer calling a package's public API directly without going through the standard app contract. Every such edge is a Zod schema producing a typed, trusted internal representation. Once data has entered through one of these edges, it flows through the rest of the system — including across package boundaries within the app — as trusted, typed data; a token-type package receiving a `TokenValue` from the core engine does not re-validate it, because the core engine already validated it at the point that value first entered the system. Re-validating internal, already-typed data is treated as a bug (redundant work, and a sign the trust boundary isn't understood), not a safety net.

- `parseTokenFile(raw: unknown): TokenDocument` (backed by a Zod schema) is the only sanctioned entry point for token JSON; nothing else calls `JSON.parse` on token content.
- A token-type package validates with Zod only at its _own_ external edges — e.g. if it exposes a standalone public API a third party could call directly, bypassing the core engine. Values passed to it through the core engine's standard internal contract (already-typed `TokenValue`s) are trusted as-is.

### Error Handling (Result Pattern)

A thrown exception is invisible to the type checker — a function's signature gives no indication it can fail — which defeats the same "can't be silently bypassed" guarantee the TypeScript Strictness constraint above establishes for types. All fallible operations therefore return a `Result<T, E>` (or `ResultAsync<T, E>` for async) from `neverthrow`, composed via `.andThen`/`.map`, rather than throwing, so a caller cannot ignore the failure case without explicitly unwrapping it. Errors fall into two categories:

- **Named errors** (e.g. `TokenParseError`) — specific to an operation, defined as a discriminated union local to the module that produces them. The caller is expected to branch on and handle these.
- **`UnknownError`** — a single shared type (in a new `@dtcg-editor/errors` package) wrapping anything unexpected surfacing from code outside our control. Not meant to be handled/branched on — only logged and surfaced.

Throwing calls (`JSON.parse`, `fetch`, third-party libraries, etc.) are wrapped into a `Result` exactly once, at the point they're called, using `fromThrowable`/`ResultAsync.fromPromise` — never left to propagate as an exception. Internal functions compose `Result`s end-to-end rather than mixing thrown and returned errors.

An `UnknownError` is logged immediately at creation — inside the same wrap helper that catches the throw — via an injected `Logger` (pino-shaped call signature; currently just `error(obj, msg?)`, with more levels expected later). The logger is passed explicitly as a parameter/context, never a module-level singleton, so it can be swapped in tests and by host apps embedding the core engine; call sites that don't supply one fall back to a `console`-backed default. Named errors are not auto-logged — that's up to whichever code handles them.

Scope: this governs engine/library code directly; the UI layer (`apps/web-app`'s Server Components and Client Component hooks) built on top of it follows the derived convention below.

#### UI-Layer Result Consumption

- **Server Components** receiving a `ResultAsync` from an engine/lib function branch on `.isOk()`/`.isErr()` and exhaustively match every named error the function's return type declares (via `instanceof`) before falling back to a generic message — the generic fallback is reserved for `UnknownError` (and any truly unmatched case), never used as a catch-all for a named error the code simply didn't branch on. See `app/tokens/[...path]/page.tsx`'s `describePageError` and `app/api/tokens/[...path]/route.ts`'s `GET`/`PATCH` handlers for the reference pattern.
- **Client Component hooks** wrapping a `fetch` call to a Route Handler never let a failed request surface as a thrown exception for the caller to catch. A `Result`/`ResultAsync` value itself never crosses the network — by the time a Client Component sees a response, it has already been translated server-side into an HTTP status code plus a JSON body (see the API table and each Route Handler's error-response shape). The hook tracks a status enum (`"idle" | "pending" | "error"`, or with a distinct `"success"` state where useful) plus an `error` field, populated by branching on `response.ok`/`response.status` and the body's `kind` discriminant, always unwrapping a failure into returned state rather than a thrown exception — mirroring the "a caller cannot ignore the failure case without explicitly unwrapping it" spirit of the Result pattern above, adapted to an already-serialized wire format. The `error` field is a discriminated union scoped to that hook's own operation (e.g. `useSaveTokenEdits`'s `SaveError`, `apps/web-app/lib/tokens/save-error.ts`) rather than a plain string, so a consuming component can render distinct UI per failure kind; a hook for a different operation defines its own analogous union rather than reusing another hook's. See `apps/web-app/hooks/useSaveTokenEdits.ts` for the reference implementation.
- **Error boundaries** (`error.tsx`) are reserved for genuinely unexpected render-time exceptions only — the UI-layer analog of `UnknownError`. Expected/named failures are always handled via the two conventions above, never by throwing and relying on a boundary to catch them. `apps/web-app/app/error.tsx` is a single root-level Client Component boundary, logging via `consoleLogger` before rendering a generic fallback with a reset action; it exists as a safety net, not a primary error-handling mechanism.

### Dependency Injection for I/O/Platform Externalities

A function that touches an I/O/platform externality — filesystem access, `fetch`, the process clock, `console`, environment variables, `process.exit`, randomness, etc. — accepts it as an explicit parameter with a real implementation as its default value, rather than importing/calling the externality directly. This generalizes the `Logger`-injection convention already established by the Error Handling constraint above to every externality category, not just logging. Inject when either holds: (a) a host app embedding the engine might need to swap the real implementation, or (b) a real call is awkward, slow, or impossible to exercise directly in a test.

**Taxonomy and injection points** (currently-used categories first, followed by categories with no call site yet):

- **`fs` read** — `readAndParseTokenFile` (`apps/web-app/lib/tokens/read.ts`), `scanTokenDirectory`/`collectJsonFiles` (`apps/web-app/lib/tokens/scan.ts`), and `loadConfig` (`apps/web-app/lib/config.ts`) each accept an injected read function, real-defaulted from `apps/web-app/lib/platform/node-fs.ts`.
- **`fs` write** — `writeAndSerializeTokenFile` (`apps/web-app/lib/tokens/write.ts`) and `runInitConfig` (`apps/web-app/scripts/init-config.ts`) each accept an injected write function, real-defaulted from the same `node-fs.ts` adapter.
- **`fetch`** — `useSaveTokenEdits.ts` accepts an injected fetch implementation; its real default (`fetch`) is declared inline at its own call site rather than via a shared adapter file, since it is the only call site for this externality (a dedicated adapter module is only introduced once a real implementation is shared across more than one call site — mirrors why `consoleLogger` exists as its own file while a single-call-site externality does not get one).
- **`process.exit` / `console.*`** — isolated in `apps/web-app/lib/fatal-startup-error.ts`'s `exitOnFatalStartupError`, reached only via `instrumentation.ts`'s `register()` dynamically importing it. This preserves the existing Edge-Runtime-safety Architecture Decision (Turbopack's Edge Runtime static-analysis pass flags Node-only APIs referenced anywhere in `instrumentation.ts`'s own top-level source, regardless of a runtime guard; only code reached exclusively through a dynamic `import()` escapes it) — `fatal-startup-error.ts`, not `instrumentation.ts` itself, is the real composition-root boundary for these two externalities.
- **`process.env`** (the `NEXT_RUNTIME` check) — read once in `instrumentation.ts`'s `register()`, the composition root, and passed into an injectable core as an env-lookup dependency.
- **Currently-unused categories, named explicitly**: `Date.now()`/`new Date()`, `Math.random()`, `crypto.randomUUID()`/`crypto.getRandomValues()`, `setTimeout`/`setInterval`. No current call site exists for any of these — the same injection requirement applies the moment one is introduced, enforced by lint rather than left to reviewer memory.

**Adapter shape**: a small module (e.g. `apps/web-app/lib/platform/node-fs.ts`) exports real, ready-to-use implementations that are used as default parameter values — the same role `consoleLogger` already plays for `Logger`. No DI container, no class wrapping; plain functions. A dedicated adapter file is only warranted when a real implementation is shared across more than one call site; a single-call-site externality declares its real default inline at that call site instead (mirrors how `apps/web-app/scripts/init-config.ts`'s `main()` already supplies real `process.argv`/`process.stdin`/`process.stdout` inline as the CLI's composition root).

**Testing-convention change**: a function with an injected I/O/platform dependency is tested by passing a mock/fake implementation through that parameter. A real fixture — a temp directory, a real network call, a real clock — is no longer the default testing approach for these, superseding this repo's prior real-temp-dir convention. Mocks are hand-rolled per-file (no shared test-utility module, no mocking library), mirroring the existing `fakeLogger()` test-helper pattern.

### Token-Type Package Contract

The core engine never hard-codes knowledge of specific token types (color, dimension, etc.); every token-type package implements a shared interface that the core engine hosts generically, so adding a new token type never requires changing the core engine. The contract itself must conform to the DTCG token format spec ([designtokens.org/tr/2025.10/format](https://www.designtokens.org/tr/2025.10/format/)); when the spec introduces breaking changes in a future version, the contract evolves in a backwards-compatible way rather than dropping support for tokens written against an earlier spec version.

Parsing and typing — turning raw DTCG JSON into the typed, validated token model — lives in its own package, separate from both the core engine and the token-type packages, and is completely agnostic of any UI or app tooling (no React, no rendering concerns). Its only real dependency is a validation library such as Zod. This is the package that owns `parseTokenFile` and the `TokenDocument`/`TokenValue` types referenced in the validation-at-the-edges constraint; the core engine and token-type packages all depend on it rather than parsing DTCG JSON themselves.

- e.g. `@dtcg-editor/token-core` (or similar) exports `parseTokenFile`, `TokenDocument`, `TokenValue`, and per-spec-type Zod schemas — no React import anywhere in this package.
- `@dtcg-editor/token-type-contract` defines the pluggable interface (`validate`, `serialize`, `render`, etc.) that token-type packages implement and the core engine hosts; this is a separate concern from the spec-parsing package above.

### Minimal Dependencies

Built-ins are the default, not a preference: reach for `Intl`, `structuredClone`, native `fetch`, and similar platform APIs before considering a library, in every package including the web app. A third-party dependency is added only when it is clearly a better alternative to hand-rolling or using a built-in — and "better" must be demonstrated, not asserted: the dependency is named and justified in the feature's plan doc (`plan.md` from the `/sdd-plan` step) before it's added, not introduced ad hoc mid-implementation. No new dependency lands without that paper trail.

- A PR/implementation that adds a new entry to any `package.json` without that dependency having been called out and justified in the corresponding `plan.md` is non-compliant, regardless of how small or popular the library is.
- Justification means stating what built-in or first-party approach was considered and why it falls short (bundle size, correctness, maintenance burden) — not just "it's popular" or "it's convenient."

### Round-Trip Fidelity

Because the DTCG spec permits extensions and tool-specific fields, the `token-core` parse/serialize cycle must be semantically lossless for anything the editor doesn't explicitly modify: parsing a token file into the internal model and re-serializing it, with no edits, must produce output with the same data as the input — including unrecognized fields and extensions — even if formatting, key ordering, or whitespace is normalized in the process. When the user edits a token, only the touched value changes semantically; everything else round-trips as the same data, even fields the internal model doesn't have a typed representation for.

- The internal token model preserves an "unknown/extension bag" per node rather than discarding anything it doesn't recognize during parsing.
- Round-trip tests are mandatory for `token-core`: parse → serialize (no edits) → re-parse and deep-equal against the original parse is a required test case for every DTCG-spec fixture file used in the test suite (comparing parsed data, not raw text, since formatting is allowed to normalize).

## Approved Dependencies

- TypeScript
- React
- Next.js (`apps/web-app`) — chosen over hand-rolling a Node server so Route Handlers + Server Components give typed, colocated server-side `fs` access without a bespoke HTTP layer.
- Biome — `tsc` has no flag that bans explicit `any`; a lint tool is required to actually enforce the "no `any`" rule in TypeScript Strictness, not just convenient. Replaced ESLint + `typescript-eslint` + `eslint-config-next` + Prettier as the repo's single lint-and-format tool: a single Rust binary covering both concerns, with its own type-inference engine that never touches `tsc`'s Compiler API — structurally immune to the TS7/Compiler-API breakage that motivated dropping `typescript-eslint`'s `typescript<6.1.0` peer-range blocker. The 11 Dependency Injection for I/O/Platform Externalities rules (this section, above) are enforced via native rules (`noExplicitAny`, `noRestrictedImports`, `noRestrictedGlobals`, the React-hooks `domains` pair) plus 7 hand-authored GritQL plugins (`biome/*.grit`) for the restricted-call-expression patterns Biome has no native equivalent for. `eslint-config-next`'s Next.js-specific lint coverage (`no-img-tag`, `no-html-link-for-pages`, etc.) has no Biome equivalent and is a permanently accepted gap — see the Migrate ESLint + Prettier to Biome feature.
- Zod (schema validation/parsing at all package edges — chosen because `z.infer` derives the static TypeScript type directly from the schema, making the schema the single source of truth for both runtime validation and the type; a hand-rolled validator plus a separately maintained interface could silently drift apart)
- neverthrow (`Result`/`ResultAsync` error handling — core, cross-cutting infrastructure per the Error Handling constraint above; justified here rather than per-feature)
- pnpm (package manager / workspaces)
- Turborepo (build orchestration)
- `@commitlint/cli` + `@commitlint/config-conventional` (commit message validation against the full Conventional Commits spec — see the Enforce Conventional Commits feature)
- `commitizen` + `cz-customizable` (interactive commit CLI, sharing the same type/scope config as commitlint)
- `husky` (installs the local `commit-msg` git hook automatically on `pnpm install`)
- `vitest` (`apps/web-app` only — `node:test` cannot execute `.tsx`/JSX at all, confirmed empirically; see the Edit Dimension Tokens in Browser feature)
- `@vitejs/plugin-react` (Vitest's standard React-JSX transform, needed alongside `vitest` since the default esbuild transform alone doesn't reliably match the automatic JSX runtime Next.js/React 19 expect)
- `jsdom` (the DOM environment Vitest renders `apps/web-app` components into — no built-in Node alternative exists)
- `@testing-library/react` (renders/queries components the way a user would; the standard pairing with Vitest for component tests, avoiding a hand-rolled DOM-assertion harness)
- `colorjs.io` (`packages/token-type-color` only — sRGB-hex ⇄ per-`colorSpace` numeric-component conversion backing the native color picker, see the Configurable Color Spaces + Interactive Color Picker feature. 6 of the 14 DTCG `colorSpace` values require correct Bradford chromatic-adaptation matrices and CSS Color 4 transfer functions per space — spec-conformance-critical math where a transcription error produces plausible-but-wrong colors, not boilerplate a built-in covers. Chosen over `culori`/`color-convert`/`colord` because it's written and maintained by the CSS Color 4/5 spec co-editors as a reference implementation — the same spec DTCG's `colorSpace` ids are drawn from verbatim — and ships `toGamut()` for the exact "clip out-of-gamut into sRGB" need the picker has. Imported via the tree-shakable `colorjs.io/fn` entry point, not the default class-based entry.)

Anything outside this list requires a flag before adding.

## Backlog & History

- `docs/backlog.md` — planned features not yet started; pick one and run `/sdd-feature` to start it.
- `docs/backlog-completed.md` — backlog items closed out by `/sdd-archive` once their feature has merged, moved out of `docs/backlog.md` so the active backlog only shows what's still open.

## Features

- **Configured Token Directory Viewer**: scans a configured directory for DTCG token files, parses each with `token-core`, and lets the user browse valid files as a navigable token tree via the Next.js web app; invalid files are flagged individually without blocking the rest. (`docs/specs-archive/202607251128-configured-token-directory-viewer/`)
- **Enforce Conventional Commits**: local `commit-msg` git hook (commitlint) and an interactive `pnpm commit` CLI (commitizen) enforce a fixed Conventional Commits type/scope standard, both reading one shared config so they can't drift; documented in `CONTRIBUTING.md`. CI-level enforcement is deferred (`docs/backlog.md`). (`docs/specs-archive/202607251245-enforce-conventional-commits/`)
- **Migrate to Result-Pattern Error Handling**: the token read chain (`token-core` parsing + all `web-app` consumers) returns `Result`/`ResultAsync` instead of throwing, per the Error Handling constraint above; a new `@dtcg-editor/errors` package provides the shared `UnknownError`/`Logger`; closes a previously-unhandled `readdir`-crash gap in `scanTokenDirectory` along the way. (`docs/specs-archive/202607251455-migrate-to-result-pattern-error-handling/`)
- **Bootstrap CI (GitHub Actions)**: a GitHub Actions workflow runs `pnpm build`/`lint`/`test` (via Turborepo) on pull requests into `main` and on pushes to `main`; type checking is covered by `build` alone (`tsc`/`next build`), so no separate typecheck step exists. CI-level Conventional Commit enforcement remains deferred (`docs/backlog.md`). (`docs/specs-archive/202607251627-bootstrap-ci-github-actions/`)
- **Edit Dimension Tokens in Browser**: Dimension tokens' name/`$value`/description are editable in the web app's token tree view and saved back to their file's JSON on disk in one batch write; every other token type stays read-only. First implementation of `token-core`'s `serialize()`/round-trip path and of the Token-Type Package Contract (`@dtcg-editor/token-type-contract` + `@dtcg-editor/token-type-dimension`, the first concrete token-type package). (`docs/specs-archive/202607270923-edit-dimension-tokens-in-browser/`)
- **Migrate config.ts to Result Pattern**: `apps/web-app/lib/config.ts`'s `loadConfig` now returns `Result<Config, ConfigError>` via `fromThrowable`/`.andThen` instead of throwing, and `instrumentation.ts`'s `register()` branches on that `Result` instead of `try`/`catch`; completes the second, previously out-of-scope call path (startup config loading) for the earlier Result-pattern migration. `getConfig()`'s public `(): Config` signature and all four request-time call sites are unchanged. (`docs/specs-archive/202607280011-migrate-config-result-pattern/`)
- **CI-Level Conventional Commit Enforcement**: a second, parallel `commitlint` job (sibling to the existing `ci` job) in `.github/workflows/ci.yml` lints real commit messages/PR commit ranges via a hand-rolled `pnpm exec commitlint --from/--to` invocation against the existing `commitlint.config.cjs` (no third-party GitHub Action, no new config); runs on both `pull_request` and `push` events, with a `git rev-list --max-parents=0` fallback for the push zero-SHA edge case. `CONTRIBUTING.md` now forbids merge commits in feature branches and requires rebasing onto `main` instead. Closes the CI-level-enforcement gap both Bootstrap CI and Enforce Conventional Commits deferred. (`docs/specs-archive/202607280019-ci-conventional-commit-enforcement/`)
- **CLI to Bootstrap `dtcg-editor.config.json`**: a new `apps/web-app/scripts/init-config.ts` command (`pnpm --filter web-app run init-config`) scaffolds `dtcg-editor.config.json`, either interactively (`node:readline/promises` prompt) or flag-drivenly (`--tokens-dir`, `--force`, `--help`, parsed via `node:util`'s `parseArgs`), validating against the same exported `ConfigFileSchema` `instrumentation.ts` uses at startup so the two can never drift. No new runtime dependency; `engines.node` bumped to `>=26.5.0` and CI's Node version bumped to `26` so the script's native `.ts` execution needs no flags. (`docs/specs-archive/202607280827-cli-bootstrap-config/`)
- **UI-Layer Result Consumption Conventions**: replaces the Error Handling constraint's prior "undefined for now" UI-layer deferral with a concrete, evidence-derived convention — Server Components exhaustively branch every named error before falling back to a generic message (`app/tokens/[...path]/page.tsx`'s `describePageError`, fixing a previously-generic `FileNotFoundError` fallback), Client Component hooks track fetch outcomes as status-enum-plus-discriminated-union state instead of thrown exceptions (`useSaveTokenEdits`, extracted from `TokenTree.tsx`'s prior ad hoc logic, as the reference implementation), and a root `app/error.tsx` boundary is scoped strictly to genuinely unexpected exceptions. `app/api/tokens/[...path]/route.ts`'s error responses gained an additive `kind` discriminant so the wire body is `SaveError`-shaped. (`docs/specs-archive/202607280829-ui-result-consumption-conventions/`)
- **Fix Edge Runtime Warning for `process.exit` in `instrumentation.ts`**: isolates the Node-only fatal-startup-exit path (`console.error` + `process.exit(1)`) behind a dynamic import of a new `apps/web-app/lib/fatal-startup-error.ts` module, mirroring the existing `config.ts` dynamic-import pattern, so Turbopack's Edge Runtime static-analysis pass no longer flags `process.exit` in `instrumentation.ts`'s own source — a structural fix, not a suppression. (`docs/specs-archive/202607280933-instrumentation-edge-runtime-process-exit/`)
- **Inject Dependencies by Default**: establishes and enforces the Dependency Injection for I/O/Platform Externalities constraint (see Architectural Constraints above) across every existing call site — `fs` (`read.ts`/`scan.ts`/`write.ts`/`config.ts`/`init-config.ts`, real-defaulted from the new `apps/web-app/lib/platform/node-fs.ts` adapter), `fetch` (`useSaveTokenEdits.ts`), and `process.exit`/`console.*`/`process.env` (`instrumentation.ts`'s new `runRegister(deps)`/`register()` split). Rewrote four fs-touching test files off real-temp-dir (`mkdtemp`) fixtures onto hand-rolled mocked-fs fakes, added the previously-missing `write.test.ts` and a new `instrumentation.test.ts`, and added ESLint `no-restricted-syntax`/`no-restricted-imports`/`no-restricted-globals` rules to both `eslint.config.mjs` files so a new direct call site outside its designated file is a lint error, not just a documented convention. No new dependency added. (`docs/specs-archive/202607281409-inject-dependencies-by-default/`)
- **Reformat Repo to Tabs**: adds Prettier (`useTabs: true`) as the repo's formatter plus `.editorconfig`/`.prettierignore`, reformats every existing Prettier-supported file (147 files) to tab indentation with zero behavioral change (`build`/`lint`/`test` verified identical before/after), and adds a `pnpm format:check` script wired into CI's existing `ci` job so formatting can't drift back to spaces. (`docs/specs-archive/202607281440-reformat-repo-to-tabs/`)
- **Editable Group Names and Visible Field Labels**: non-root groups in the token tree (`TokenTree.tsx`) become renameable through the same batched edit/save pipeline tokens already use — `token-core`'s `applyTokenEdits` gained group-rename support (with cascading descendant `path` updates and a depth-based stable sort fixing a same-batch ordering hazard), mirrored client-side in `edit-state.ts`, and accepted by `route.ts` for name-only group edits. Every field in the tree (editable and read-only, including `DimensionEditor`'s value/unit inputs) now has a real visible `<label>` instead of relying solely on `aria-label`, preserving today's per-row accessible-name uniqueness. (`docs/specs-archive/202607281844-editable-group-names-and-visible-field-labels/`)
- **Save Button as a Call-to-Action Component**: extracts the token tree's inline `<button>` into its own `apps/web-app/components/SaveButton.tsx` component (+ `SaveButton.module.css`), restyled as a larger, rounded, solid-accent CTA with `:hover`/`:focus-visible`/`:disabled` states and a hand-rolled inline disk-icon SVG (no new dependency). New `--accent`/`--accent-hover`/`--accent-foreground` CSS custom properties added to `globals.css` (light + dark), introduced generically for future CTAs. Accessible name and `disabled` semantics unchanged; existing `TokenTree.test.tsx` assertions pass unmodified. (`docs/specs-archive/202607282347-save-button-as-a-call-to-action-component/`)
- **Config-Driven Token Editor Components**: `apps/web-app/dtcg-editor.config.mts` (committed, calling a new `defineConfig({ tokensDir, extensions })` helper) replaces `dtcg-editor.config.json`; `extensions` is an array of `{ filter, editor }` pairs letting a user override which React component edits a subset of tokens, merged by `defineConfig` with this repo's built-in defaults (currently just `DimensionEditor` for `"dimension"`, sourced from `@dtcg-editor/token-type-dimension`'s `dimensionTokenType`) so zero-config behavior is unchanged. `TokenTree.tsx` resolves the editor per token via `resolveEditorForType` (first-match-wins, user entries ordered ahead of defaults) instead of a hard-coded `DimensionEditor` import; `edit-state.ts`/`route.ts` validation/serialization untouched (UI-selection-only scope). (`docs/specs-archive/202607292258-config-driven-token-editor-components/`)
- **Prettier Pre-Commit Formatting**: a new `.husky/pre-commit` hook runs `format-staged.cjs`, which gets the commit's staged files from git, runs `prettier --write --ignore-unknown` on exactly that set, and re-stages the result — so a commit can never introduce unformatted code without a manual `pnpm format` step. Hand-rolled over adding `lint-staged` as a dependency, per the Minimal Dependencies constraint; git/prettier calls are injected via an `exec` parameter for unit testability. (`docs/specs-archive/202607300736-prettier-pre-commit-formatting/`)
- **Accessibility Testing**: two-tier automated WCAG 2.2 AA checking for `apps/web-app` — a Vitest Browser Mode project (`@vitest/browser` + `@vitest/browser-playwright`, real Chromium) runs `axe-core` against `FolderOverview`, `SaveButton`, `TokenTree`, and the dimension token editor; a `@playwright/test` suite checks the home/tokens/error pages plus a full keyboard-only browse → open → edit → save flow. Both are enforced (non-warn-only) under `pnpm test`/`turbo run test` and wired into CI. Found and fixed two real issues along the way: a WCAG 1.4.3 color-contrast violation in `TokenTree`'s field labels, and a `process is not defined` crash from `next/link`'s internals in the real-browser test environment. (`docs/specs-archive/202608011523-accessibility-testing/`)
- **Generic Fallback Token Editor**: any token whose `$type` is a spec-valid DTCG type (`packages/token-core`'s new canonical `DTCG_TOKEN_TYPES` list) but has no custom editor registered becomes editable via a generic JSON-text editor; tokens with a non-DTCG `$type` stay read-only and are now flagged as "non-standard" in `FolderOverview` rather than silently treated like any other uneditable token. `TokenEditorExtension` changes from `{ filter, editor }` to `{ type, editor }`, with `defineConfig` validating `type` against the canonical list at config-load time; both client (`TokenTree.tsx`'s `canEdit`) and server (`route.ts`'s edit-authorization gate) generalize off their prior dimension-only hard-coding. (`docs/specs-archive/202608012104-generic-fallback-token-editor/`)
- **Support for Colour (Color) Tokens**: adds `@dtcg-editor/token-type-color` for DTCG's `color` `$type` (all 14 2025.10 Color module colorSpaces + legacy bare-hex `$value`), a CSS-native swatch preview, non-blocking per-colorSpace range validation, and a full `ColorEditor` UI. Originally shipped read-only; once `fallback-token-editor` merged and generalized `canEdit`, a follow-up fix (this same worktree) extended that generalization to validate any built-in type's value against its own contract schema — both client-side (`TokenTree.tsx`) and server-side (`route.ts`'s PATCH handler, which previously validated only `dimension` edits) — before allowing/accepting an edit, unlocking color-token editing safely and closing a validation gap that predated this feature. (`docs/specs-archive/202608021545-support-for-colour-tokens/`)
- **Configurable Color Spaces + Interactive Color Picker**: lets a host app restrict which of the 14 DTCG `colorSpace` values `ColorEditor` offers, via a new generic `editorOptions`/`editorOptionsSchema` config channel (`TokenEditorExtension.editorOptions` validated against `TokenTypeContract.editorOptionsSchema` by `defineConfig`) and `token-type-color`'s `ColorEditorOptions`/`defineColorConfig`. Also adds a native `<input type="color">` picker to both the object and legacy-hex color editors, backed by `colorjs.io` for sRGB-hex ⇄ any-colorSpace conversion, derived fresh from the current value on every render so it can't drift from the numeric/hex fields; picking never touches alpha. `TokenEditorExtension.type` reverts from plain `string` back to the `DtcgTokenType` union along the way. (`docs/specs-archive/202608022222-configurable-color-spaces-interactive-color-picker/`)
- **Migrate ESLint + Prettier to Biome**: replaces ESLint (`typescript-eslint`, `eslint-config-next`) and Prettier repo-wide with Biome 2.5.8 as the single lint-and-format tool; all 11 Dependency Injection for I/O/Platform Externalities rules (see the constraint above) reproduced 1:1 via native Biome rules (`noExplicitAny`, `noRestrictedImports`, `noRestrictedGlobals`, the `domains.react` hooks pair) plus 7 hand-authored GritQL plugins (`biome/*.grit`) for the restricted-call-expression patterns Biome has no native equivalent for, with every documented per-file exemption preserved. `eslint-config-next`'s Next.js-specific lint coverage is a permanently accepted, documented gap (no Biome plugin ecosystem or third-party fill exists). First of two sequenced features resolving the "Upgrade `typescript` to v7" backlog item — removes `typescript-eslint`'s `typescript<6.1.0` peer-range blocker, since Biome's own type-inference engine never touches `tsc`'s Compiler API. (`docs/specs-archive/202608161600-migrate-eslint-prettier-to-biome/`)

## Architecture Decisions

| Date       | Decision                                                                                                                                                                                                                                                                                                                                                                                                                 | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Feature                                                                                                                                       |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-25 | `packages/*` (installable libraries, e.g. `token-core`) vs `apps/*` (deployable apps, e.g. `web-app`) directory split                                                                                                                                                                                                                                                                                                    | Matches the "installable module" framing already used for engine/parsing packages vs. the one deployable web app surface                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [Configured Token Directory Viewer](docs/specs-archive/202607251128-configured-token-directory-viewer/)                                       |
| 2026-07-25 | Startup config validation runs in Next.js's `instrumentation.ts` `register()` hook, failing fast with `process.exit(1)` on a missing/invalid config                                                                                                                                                                                                                                                                      | App Router has no traditional server "main" function; `instrumentation.ts` is the idiomatic one-time startup hook (Node runtime only)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | [Configured Token Directory Viewer](docs/specs-archive/202607251128-configured-token-directory-viewer/)                                       |
| 2026-07-25 | Route Handlers use standard `Response.json()`, not `NextResponse.json()`                                                                                                                                                                                                                                                                                                                                                 | `next/server` has no ESM `exports` map, so `NextResponse` only resolves under Node's legacy CJS resolution — this broke `node --test` importing route modules directly; `Response.json()` is a standard Fetch API method needing no import                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | [Configured Token Directory Viewer](docs/specs-archive/202607251128-configured-token-directory-viewer/)                                       |
| 2026-07-25 | `token-core`'s Round-Trip Fidelity constraint is only partially applied so far: `serialize()` and round-trip tests are not yet built                                                                                                                                                                                                                                                                                     | This feature is read-only, so there's nothing to round-trip yet; the internal model already preserves an unrecognized-field "extension bag" per node so `serialize()` can be added later without re-touching the parse model — flagged here as a deliberate, temporary gap, not an oversight                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | [Configured Token Directory Viewer](docs/specs-archive/202607251128-configured-token-directory-viewer/)                                       |
| 2026-07-25 | Turborepo's `//#<task>` root-task syntax is the standing pattern for wiring root-only scripts into `pnpm build`/`lint`/`test`, since `pnpm-workspace.yaml` only globs `packages/*`/`apps/*` and the repo root is otherwise invisible to Turborepo's pipeline                                                                                                                                                             | Established via `//#test:commits` and `//#lint:root`; reusable for any future root-level tooling, not just this feature                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | [Enforce Conventional Commits](docs/specs-archive/202607251245-enforce-conventional-commits/)                                                 |
| 2026-07-25 | `eslint.config.mjs` exempts `**/*.cjs` files from `@typescript-eslint/no-require-imports`                                                                                                                                                                                                                                                                                                                                | Root-level tooling configs (commitlint, cz-customizable) must be plain CommonJS for third-party `require()`-based config loading to work, unlike every TS package in this monorepo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | [Enforce Conventional Commits](docs/specs-archive/202607251245-enforce-conventional-commits/)                                                 |
| 2026-07-25 | Known-vs-unknown error classification (per the Error Handling constraint) is decided by auditing what the code _currently_ handles, not a speculative taxonomy                                                                                                                                                                                                                                                           | Evidence-based, not invented — every error path already deliberately handled today (e.g. `TokenParseError`, ENOENT) stays a named error; only genuinely-unhandled-today paths (e.g. `readdir` failures) become `UnknownError`. Reusable methodology for future Result-pattern migrations, e.g. the still-open "migrate `config.ts`" backlog item                                                                                                                                                                                                                                                                                                                                                                                                                | [Migrate to Result-Pattern Error Handling](docs/specs-archive/202607251455-migrate-to-result-pattern-error-handling/)                         |
| 2026-07-25 | A Next.js Route Handler needing an injectable dependency (e.g. a `Logger`) for testing splits its logic into a separately-exported, injectable function that the exported `GET`/`POST`/etc. wraps, rather than changing the handler's own signature                                                                                                                                                                      | Next.js's generated route types constrain the exported HTTP-verb functions to their exact expected signature (`next build` fails type-checking otherwise) — confirmed by trying to add a second parameter directly to `GET` first. Directly relevant to, and generalized by, the Inject Dependencies by Default feature                                                                                                                                                                                                                                                                                                                                                                                                                                         | [Migrate to Result-Pattern Error Handling](docs/specs-archive/202607251455-migrate-to-result-pattern-error-handling/)                         |
| 2026-07-25 | GitHub Actions workflows install pnpm via `corepack enable` (reading the `packageManager` field) plus `actions/setup-node`'s built-in `cache: pnpm`, rather than a third-party action like `pnpm/action-setup` or manual `actions/cache` wiring                                                                                                                                                                          | Keeps the workflow's action surface limited to GitHub-maintained actions, consistent with the Minimal Dependencies constraint applied to CI tooling as well as package dependencies; reusable pattern for any future workflow (release, docs deploy, etc.) added to this repo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | [Bootstrap CI (GitHub Actions)](docs/specs-archive/202607251627-bootstrap-ci-github-actions/)                                                 |
| 2026-07-25 | CI relies on `pnpm build` as the sole type-checking gate (no separate `tsc --noEmit`/typecheck step)                                                                                                                                                                                                                                                                                                                     | Every package's `build` script already fails on type errors — `tsc -p tsconfig.json` for `token-core`/`errors`, `next build` for `web-app` — so a dedicated step would redundantly re-check the same source; load-bearing assumption that breaks silently if a future `next.config.ts` change sets `typescript.ignoreBuildErrors`                                                                                                                                                                                                                                                                                                                                                                                                                               | [Bootstrap CI (GitHub Actions)](docs/specs-archive/202607251627-bootstrap-ci-github-actions/)                                                 |
| 2026-07-27 | Round-Trip Fidelity is satisfied by rebuilding the tree and `JSON.stringify`-ing it, not by source-preserving/AST-based editing                                                                                                                                                                                                                                                                                          | The constraint only requires the _data_ to round-trip, not formatting/key ordering — closes the gap flagged as deliberate-but-temporary when the read-only viewer shipped without `serialize()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | [Edit Dimension Tokens in Browser](docs/specs-archive/202607270923-edit-dimension-tokens-in-browser/)                                         |
| 2026-07-27 | `token-core` stays completely type-agnostic even for editing: the "is this token's type currently editable" policy lives in `apps/web-app`, not `token-core`                                                                                                                                                                                                                                                             | Matches the Token-Type Package Contract principle that the core engine never hard-codes a specific type; adding the next editable token type is an app-layer change only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [Edit Dimension Tokens in Browser](docs/specs-archive/202607270923-edit-dimension-tokens-in-browser/)                                         |
| 2026-07-27 | `apps/web-app` is the only package on Vitest; `packages/*` (including the new `token-type-*` packages) stay on `node:test`                                                                                                                                                                                                                                                                                               | Scoped to exactly where the concrete gap exists — only `apps/web-app` renders/tests JSX components                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | [Edit Dimension Tokens in Browser](docs/specs-archive/202607270923-edit-dimension-tokens-in-browser/)                                         |
| 2026-07-27 | Client-side token edits apply optimistically to a local copy of the tree rather than via a full Server Component refresh                                                                                                                                                                                                                                                                                                 | Avoids a timing race between clearing "pending" UI state and a server re-fetch landing, at the cost of a second, parallel "apply an edit immutably" implementation (client array-based `PlainDtcgNode`, server `Map`-based `TokenDocument`) — mirrors the existing `DtcgNode`/`PlainDtcgNode` split already in the codebase                                                                                                                                                                                                                                                                                                                                                                                                                                     | [Edit Dimension Tokens in Browser](docs/specs-archive/202607270923-edit-dimension-tokens-in-browser/)                                         |
| 2026-07-27 | `instrumentation.ts`'s `register()` calls `loadConfig()` directly (not `getConfig()`) and, on success, pre-warms `getConfig()`'s request-time memoization cache via a new exported `setConfigCache()`                                                                                                                                                                                                                    | Once `register()` branches on `loadConfig()`'s `Result` itself, there's no other way to populate `config.ts`'s private module-level cache before any request arrives; keeps `getConfig()`'s public `(): Config` signature — and all four request-time call sites — completely unchanged. Reusable pattern for any future startup-populated, request-time-memoized value                                                                                                                                                                                                                                                                                                                                                                                         | [Migrate config.ts to Result Pattern](docs/specs-archive/202607280011-migrate-config-result-pattern/)                                         |
| 2026-07-27 | A should-be-unreachable defensive-programming branch (`getConfig()`'s cache-miss/`Err` fallback) still throws a dedicated named `Error` subclass (`ConfigNotInitializedError`), not a bare `throw new Error(...)`, even though it sits outside the `Result` chain and is provably unreachable under normal operation                                                                                                     | Keeps this codebase's "always a named error type, never an ad hoc bare `Error`" convention uniform across both fallible-operation `Result`s and defensive assertions, rather than carving out an exception for the latter                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | [Migrate config.ts to Result Pattern](docs/specs-archive/202607280011-migrate-config-result-pattern/)                                         |
| 2026-07-28 | CI-side Conventional Commit enforcement is hand-rolled against the existing `@commitlint/cli` devDependency (`pnpm exec commitlint --from/--to`) rather than adopting a third-party GitHub Action (e.g. `wagoid/commitlint-github-action`)                                                                                                                                                                               | Stays consistent with the Minimal Dependencies constraint and Bootstrap CI's GitHub-actions-only precedent; reuses the exact config the local hook and `test:commits` already use, so the three enforcement points can't drift apart                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | [CI-Level Conventional Commit Enforcement](docs/specs-archive/202607280019-ci-conventional-commit-enforcement/)                               |
| 2026-07-28 | Commit-message linting runs as a new sibling `commitlint` job in `.github/workflows/ci.yml`, not a new step in the existing `ci` job, with its own `fetch-depth: 0` checkout                                                                                                                                                                                                                                             | Gives an independently-attributable check in the GitHub PR UI and isolates the deeper checkout this feature needs from the existing build/lint/test job, at the cost of duplicating the checkout/corepack/setup-node/install sequence (accepted per `feature.md`'s Non-Functional Requirements)                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | [CI-Level Conventional Commit Enforcement](docs/specs-archive/202607280019-ci-conventional-commit-enforcement/)                               |
| 2026-07-28 | `CONTRIBUTING.md` forbids merge commits in feature branches; contributors must rebase onto `main` instead                                                                                                                                                                                                                                                                                                                | This repo rebase-merges PRs, so every individual commit lands permanently on `main`'s history; CI's commit-message check tolerates/skips merge commits defensively via commitlint's default-ignore behavior, but that tolerance is a backstop, not an invitation to merge freely                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | [CI-Level Conventional Commit Enforcement](docs/specs-archive/202607280019-ci-conventional-commit-enforcement/)                               |
| 2026-07-28 | `.github/workflows/ci.yml` gained a workflow-level `concurrency` group (`group: ${{ github.workflow }}-${{ github.ref }}`, `cancel-in-progress: true`)                                                                                                                                                                                                                                                                   | Cancels superseded runs on the same ref (e.g. rapid PR pushes) instead of letting stale runs race to completion; added per `/sdd-review`'s Minor finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [CI-Level Conventional Commit Enforcement](docs/specs-archive/202607280019-ci-conventional-commit-enforcement/)                               |
| 2026-07-28 | A new CLI script (`apps/web-app/scripts/init-config.ts`) follows the injectable-core/thin-wrapper split: `runInitConfig(io: InitConfigIO): Promise<Result<string, string>>` holds 100% of the behavior, and a `main()` plus ESM "run only if executed directly" guard is the only code touching real `process.argv`/`process.stdin`/`process.stdout`/`process.exit`                                                      | Mirrors the existing Route Handler precedent (a handler needing an injectable dependency splits into a separately-exported function the handler wraps); makes every interactive/re-prompt/existing-file branch testable via Vitest directly against injected streams, no child-process spawning                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | [CLI to Bootstrap dtcg-editor.config.json](docs/specs-archive/202607280827-cli-bootstrap-config/)                                             |
| 2026-07-28 | CLI flag parsing uses `node:util`'s built-in `parseArgs`; interactive prompting uses `node:readline/promises` — no third-party CLI/prompt library dependency                                                                                                                                                                                                                                                             | Both are Node built-ins requiring no Minimal Dependencies sign-off (that constraint governs third-party packages); consistent with the "built-ins are the default" bias applied elsewhere in the repo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | [CLI to Bootstrap dtcg-editor.config.json](docs/specs-archive/202607280827-cli-bootstrap-config/)                                             |
| 2026-07-28 | Root `package.json`'s `engines.node` bumped `>=20` → `>=26.5.0` (current latest published Node.js release), with `.github/workflows/ci.yml`'s two `actions/setup-node` steps bumped `"22"` → `"26"` in the same change                                                                                                                                                                                                   | Lets the new CLI script's native `.ts` type-stripping run unflagged with no `--experimental-strip-types`; `engines` itself is advisory (no `engine-strict` setting in this repo), so CI's own Node version bump is the real enforcement gate, not the local `engines` field                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | [CLI to Bootstrap dtcg-editor.config.json](docs/specs-archive/202607280827-cli-bootstrap-config/)                                             |
| 2026-07-28 | `useSaveTokenEdits`'s `SaveError` is parsed from a Route Handler response body via a direct `as SaveError` cast, not runtime Zod validation                                                                                                                                                                                                                                                                              | This specific edge (own-app Route Handler response) is a same-codebase wire contract fully controlled by this feature's own two halves (`route.ts`'s `errorResponse` and the hook's parser), introduced together — not a genuinely external edge per the Validation at the Edges constraint's stated examples (file reads, third-party calls); simplified from an initial hand-rolled field-by-field type guard per `/sdd-review` human decision                                                                                                                                                                                                                                                                                                                | [UI-Layer Result Consumption Conventions](docs/specs-archive/202607280829-ui-result-consumption-conventions/)                                 |
| 2026-07-28 | `useSaveTokenEdits` (`apps/web-app/hooks/useSaveTokenEdits.ts`) is the reference implementation of the Client Component hook-state convention                                                                                                                                                                                                                                                                            | Any future hook wrapping a `fetch`-backed operation should follow its status-enum-plus-discriminated-union-error shape and define its own analogously-scoped error union rather than reusing `SaveError` for an unrelated request                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | [UI-Layer Result Consumption Conventions](docs/specs-archive/202607280829-ui-result-consumption-conventions/)                                 |
| 2026-07-28 | A single root-level `apps/web-app/app/error.tsx` Client Component boundary is scoped strictly to genuinely unexpected render-time exceptions (the UI-layer analog of `UnknownError`), never used for expected/named failures                                                                                                                                                                                             | Named/expected failures are always handled via Server Component branching or hook state per the UI-Layer Result Consumption convention; the boundary exists as a safety net, not a primary error-handling mechanism — no per-route boundaries added                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | [UI-Layer Result Consumption Conventions](docs/specs-archive/202607280829-ui-result-consumption-conventions/)                                 |
| 2026-07-28 | `route.ts`'s `GET`/`PATCH` handlers share one `mapReadErrorToResponse` helper for translating a `readAndParseTokenFile` failure into a `SaveError`-shaped response, instead of each handler hand-rolling the same `instanceof` chain                                                                                                                                                                                     | Removes a duplicated `PathTraversalError`/`FileNotFoundError`/`TokenParseError`/fallback branch that `/sdd-review` flagged as drift-prone; both handlers now can't diverge on status codes or `kind`/`error`/`details` fields for the same underlying failure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | [UI-Layer Result Consumption Conventions](docs/specs-archive/202607280829-ui-result-consumption-conventions/)                                 |
| 2026-07-28 | Node-only failure paths that would otherwise sit inline in `instrumentation.ts`'s own top-level source (e.g. the fatal-startup `process.exit(1)` call) must be isolated behind their own dynamic `import()`, mirroring the existing `config.ts` pattern, rather than relying on the `NEXT_RUNTIME !== "nodejs"` runtime guard alone                                                                                      | Turbopack bundles `instrumentation.ts` for both the Node and Edge runtimes and its Edge Runtime static-analysis pass scans the file's own source for unsupported Node APIs regardless of a runtime `if` guard; only code reached exclusively via dynamic `import()` escapes that scan. Reusable pattern for any future Node-only addition to `register()`                                                                                                                                                                                                                                                                                                                                                                                                       | [Fix Edge Runtime Warning for process.exit in instrumentation.ts](docs/specs-archive/202607280933-instrumentation-edge-runtime-process-exit/) |
| 2026-07-28 | `apps/web-app/lib/fatal-startup-error.ts`, not `instrumentation.ts`, is the real ESLint exemption target for direct `process.exit`/`console.*` calls                                                                                                                                                                                                                                                                     | Corrects a gap in this feature's own file audit: the Edge Runtime fix (row above) already moved these two externalities' only real call site into `fatal-startup-error.ts`, reached solely via `instrumentation.ts`'s dynamic `import()`; exempting `instrumentation.ts` itself instead would enforce nothing, since it no longer contains a direct call to either                                                                                                                                                                                                                                                                                                                                                                                              | [Inject Dependencies by Default](docs/specs-archive/202607281409-inject-dependencies-by-default/)                                             |
| 2026-07-28 | `instrumentation.ts`'s injectable `runRegister(deps)` core takes a single `onFatalError(message)` dependency (combining FR-05's illustrative separate exit/log deps into one), whose real implementation is constructed only inside a dynamic `import()` callback and never referenced as a bare value at the file's top level                                                                                           | Referencing `process.exit`/`console.error` as bare top-level values anywhere in `instrumentation.ts`'s own source — even just to build a default parameter — would re-trip the same Edge Runtime static-analysis warning the earlier fix (row above) resolved, regardless of a runtime guard                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | [Inject Dependencies by Default](docs/specs-archive/202607281409-inject-dependencies-by-default/)                                             |
| 2026-07-28 | The DI-convention ESLint rules (`no-restricted-syntax`/`no-restricted-imports`/`no-restricted-globals`) are added to `apps/web-app/eslint.config.mjs` (its own local flat-config file), not only the root `eslint.config.mjs`                                                                                                                                                                                            | `apps/web-app` has its own `eslint.config.mjs` (extends `eslint-config-next`), and ESLint flat-config resolution walks up from a package's cwd to its _nearest_ config file — for `apps/web-app`'s `lint` script that's the local file, not the root one. Every real call site this feature restricts lives inside `apps/web-app`; adding the rules only at the root would apply them to `packages/*` (zero call sites, vacuous) and never fire on the code they target. The zero-exemption `Date.now`/`Math.random`/`crypto.*` bans are added to both files, since those apply repo-wide                                                                                                                                                                       | [Inject Dependencies by Default](docs/specs-archive/202607281409-inject-dependencies-by-default/)                                             |
| 2026-07-28 | `pnpm format:check` (Prettier) is added as a new step in the existing `.github/workflows/ci.yml` `ci` job, not a new sibling job                                                                                                                                                                                                                                                                                         | Unlike the `commitlint` job (which needed a deep `fetch-depth: 0` checkout), `format:check` is a fast, stateless check with no special checkout needs — no reason to pay for a second job's checkout/corepack/setup-node/install sequence; reusable pattern for any future fast repo-wide check                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | [Reformat Repo to Tabs](docs/specs-archive/202607281440-reformat-repo-to-tabs/)                                                               |
| 2026-07-28 | A batch of edits applied via `token-core`'s `applyTokenEdits` is stably sorted by descending `path.length` before being applied sequentially, rather than trusting caller-supplied array order                                                                                                                                                                                                                           | Once a group (not just a leaf token) can be renamed in the same batch as an edit to one of its descendants, applying edits in raw array order can invalidate a descendant's `path` before its own edit runs if the ancestor's rename happens to come first; a stable depth-sort guarantees a descendant edit always resolves before an edit renaming one of its ancestors, with zero behavior change for the common all-same-depth case. The client's `edit-state.ts` mirrors the identical sort for its own optimistic-apply pass, so it can't diverge from what the server actually did for the same batch                                                                                                                                                    | [Editable Group Names and Visible Field Labels](docs/specs-archive/202607281844-editable-group-names-and-visible-field-labels/)               |
| 2026-07-28 | A field's visible label uses the HTML implicit-association pattern (`<label>text<input/></label>`, control nested inside the label) rather than generating a unique `id` and pairing it with `htmlFor`                                                                                                                                                                                                                   | Needs zero new props on `DimensionEditor`/`TokenTypeEditorProps` and no id-collision bookkeeping across multiple rendered rows of the same token type; per-row accessible-name uniqueness is still preserved by keeping each label's own text per-node-specific (e.g. `"small name"`) rather than a generic caption, so switching every field from `aria-label`-only to a real visible label didn't regress `getByLabelText` uniqueness anywhere. Establishes the pattern any future token-type editor's fields should follow                                                                                                                                                                                                                                   | [Editable Group Names and Visible Field Labels](docs/specs-archive/202607281844-editable-group-names-and-visible-field-labels/)               |
| 2026-07-28 | New `--accent`/`--accent-hover`/`--accent-foreground` CSS custom properties added to `apps/web-app/app/globals.css` (light + dark variants), the first accent color in the codebase, introduced generically rather than scoped only to the Save button                                                                                                                                                                   | Makes the accent themeable and reusable for future primary-action CTAs beyond the Save button, following the same `@media (prefers-color-scheme: dark)` pattern the existing `--background`/`--foreground`/`--border`/`--error` variables use                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | [Save Button as a Call-to-Action Component](docs/specs-archive/202607282347-save-button-as-a-call-to-action-component/)                       |
| 2026-07-29 | `dtcg-editor.config.mts` is committed to the repo, not gitignored the way `dtcg-editor.config.json` was                                                                                                                                                                                                                                                                                                                  | `extensions`' editor component references must be statically resolvable at build time to reach the client bundle, so the config file can no longer be optional/machine-specific the way a runtime-only data file could — a gitignored copy breaks `next build`/`vitest`/`tsc` entirely on a fresh clone or in CI, confirmed empirically                                                                                                                                                                                                                                                                                                                                                                                                                         | [Config-Driven Token Editor Components](docs/specs-archive/202607292258-config-driven-token-editor-components/)                               |
| 2026-07-29 | The config module format is `.mts` only; `.mjs` support was scoped out                                                                                                                                                                                                                                                                                                                                                   | Turbopack's bundler resolves an extensionless import but this repo's `tsc`-based type-check gate (the sole type-checking gate, per the 2026-07-25 decision above) does not, for either extension — a literal-extension import can only ever target one format, and genuine dual-format support would need a pre-build codegen step; declined in favor of the simpler single-extension path                                                                                                                                                                                                                                                                                                                                                                      | [Config-Driven Token Editor Components](docs/specs-archive/202607292258-config-driven-token-editor-components/)                               |
| 2026-07-29 | `lib/config.ts` resolves the user config via a _static_ top-level import (not a dynamic import inside `loadConfig`), and `getConfig()`'s cache-miss fallback to `loadConfig()` is required, not defensive dead code                                                                                                                                                                                                      | Next.js/Turbopack bundles `instrumentation.ts`'s dynamic `import("./lib/config.ts")` and a page/Route Handler's static import of the same file into **separate chunks**, each with independent module-level state — `register()`'s `setConfigCache()` in one chunk was never visible to `getConfig()` in another, confirmed by running the real `next dev`/`next start` servers, not just unit tests. A prior "should be unreachable in normal operation" fallback (added by the earlier Migrate config.ts to Result Pattern feature) had silently been load-bearing all along; removing it as dead code was a real regression before this was understood. Reusable gotcha for any future feature relying on `instrumentation.ts`-populated shared module state | [Config-Driven Token Editor Components](docs/specs-archive/202607292258-config-driven-token-editor-components/)                               |
| 2026-07-29 | New extensible editor-registry pattern: `TokenEditorExtension { filter, editor }` pairs, merged by `defineConfig` (user-supplied entries ordered ahead of built-in defaults) and resolved via first-match-wins (`resolveEditorForType`)                                                                                                                                                                                  | Establishes the mechanism for future per-token-type UI customization without hard-coding a specific type into `TokenTree.tsx`; `BUILT_IN_TOKEN_TYPES` (`apps/web-app/lib/token-editors/built-in.ts`) is the single source of truth a future token-type package's addition must extend, kept honest by a TypeScript mapped-type record rather than two independently-maintained lists                                                                                                                                                                                                                                                                                                                                                                            | [Config-Driven Token Editor Components](docs/specs-archive/202607292258-config-driven-token-editor-components/)                               |
| 2026-07-30 | Hand-rolled `format-staged.cjs` over adding `lint-staged` as a dependency for the pre-commit auto-format hook                                                                                                                                                                                                                                                                                                            | `lint-staged`'s only capability beyond what's needed here is hunk-level partial-stage isolation, which this feature's `feature.md` (FR-03) explicitly accepts as a known limitation rather than a requirement — nothing left needs it that `git`/`prettier` CLI plumbing via `child_process` can't already do, per the Minimal Dependencies constraint                                                                                                                                                                                                                                                                                                                                                                                                          | [Prettier Pre-Commit Formatting](docs/specs-archive/202607300736-prettier-pre-commit-formatting/)                                             |
| 2026-07-30 | `core.hooksPath` resolves relative to the repository's primary checkout, not the current linked worktree — a hook committed only to a feature branch is inert for commits made in _any_ linked worktree until the primary checkout's checked-out branch itself has that hook file on disk                                                                                                                                | Discovered empirically during this feature's `/sdd-review`: a real `git commit` made in the feature's own worktree silently skipped the newly-added `.husky/pre-commit` hook with no error. Standing caveat for any future husky hook added from a worktree-based feature branch, not just this one — see `CONTRIBUTING.md`'s "Git worktree caveat"                                                                                                                                                                                                                                                                                                                                                                                                             | [Prettier Pre-Commit Formatting](docs/specs-archive/202607300736-prettier-pre-commit-formatting/)                                             |
| 2026-08-01 | Component-level a11y checks use a second Vitest `test.projects` entry (`a11y`) running in a real Chromium instance via `@vitest/browser`'s `playwright()` provider, imported from the separate `@vitest/browser-playwright` package — not the bare `"playwright"` string older docs describe                                                                                                                             | The installed `vitest@^4.1.10`'s own `.d.ts` files require the factory, not a string, for `browser.provider`; jsdom cannot produce accurate `getComputedStyle`/layout for `axe-core`'s rendering-dependent rules (e.g. `color-contrast`), so a real browser is required for this suite specifically, alongside the unchanged jsdom `unit` project. Establishes the pattern any future component's a11y test follows                                                                                                                                                                                                                                                                                                                                             | [Accessibility Testing](docs/specs-archive/202608011523-accessibility-testing/)                                                               |
| 2026-08-01 | Text meant to read as de-emphasized/secondary uses an explicit CSS custom property (`--muted-foreground`), not `opacity` applied to the base `--foreground` color                                                                                                                                                                                                                                                        | `TokenTree.module.css`'s `.fieldLabel` (`opacity: 0.5` on `--foreground`) computed to a 3.94:1 contrast ratio against `--background`, below the WCAG 1.4.3 AA minimum of 4.5:1 — caught by the new `a11y` Vitest project. `--muted-foreground` (`#595959` light / `#a8a8a8` dark) is independently verified >7:1 in both themes; any future de-emphasized text should use this property rather than reintroducing opacity-based dimming                                                                                                                                                                                                                                                                                                                         | [Accessibility Testing](docs/specs-archive/202608011523-accessibility-testing/)                                                               |
| 2026-08-01 | Real-browser Vitest projects (the new `a11y` project) set `define: { "process.env": {} }`, scoped to that project only                                                                                                                                                                                                                                                                                                   | `next/link`'s internals (e.g. `has-base-path.js`) read `process.env.__NEXT_ROUTER_BASEPATH` at module scope, which a real Next.js build inlines via DefinePlugin but which throws `ReferenceError: process is not defined` with no Node `process` global in a real-browser test runner. Reusable fix for any future component test that transitively imports `next/link` (or similar) under the `a11y` project                                                                                                                                                                                                                                                                                                                                                  | [Accessibility Testing](docs/specs-archive/202608011523-accessibility-testing/)                                                               |
| 2026-08-01 | A dedicated test-only route (`apps/web-app/app/error-boundary-check/page.tsx`) unconditionally throws, solely so `app/error.tsx`'s root error boundary has a real page to be exercised against by Playwright                                                                                                                                                                                                             | This repo's Result-pattern discipline (see the Error Handling constraint above) means there is no organic, user-reachable path that produces an unhandled exception — `getConfig()`'s own defensive throw is provably unreachable in normal operation. **Gotcha**: this route must not use a leading-underscore folder name (`_foo`/`__foo`) — Next.js treats those as private, non-routable folders, which 404's the route until renamed                                                                                                                                                                                                                                                                                                                       | [Accessibility Testing](docs/specs-archive/202608011523-accessibility-testing/)                                                               |
| 2026-08-01 | The canonical DTCG `$type` list (`DTCG_TOKEN_TYPES`, `packages/token-core/src/token-types.ts`) is the single source both client editor-resolution and server edit-authorization check against — no second, independently-maintained list                                                                                                                                                                                 | Prevents client/server drift on "what counts as a standard type"; doc-commented with the exact DTCG 2025.10 spec URL/version it was sourced from so a future spec bump has one obvious update point                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | [Generic Fallback Token Editor](docs/specs-archive/202608012104-generic-fallback-token-editor/)                                               |
| 2026-08-01 | `TokenEditorExtension.type` is typed as plain `string`, not the `DtcgTokenType` union; `defineConfig` is the actual (runtime) enforcement point                                                                                                                                                                                                                                                                          | A config author authoring `.mts` can always write a wrong string literal regardless of the declared type, so the union adds no real safety — keeps `types.ts` free of a compile-time dependency on `token-core`'s exact union beyond what `defineConfig` already needs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [Generic Fallback Token Editor](docs/specs-archive/202608012104-generic-fallback-token-editor/)                                               |
| 2026-08-01 | The generic fallback editor's `value`/`onChange` carry the `$value`'s JSON _text_, not the parsed value — the component itself does zero parsing/validation; `TreeNode` in `TokenTree.tsx` owns the `JSON.parse`/try-catch and error surfacing                                                                                                                                                                           | Keeps validation logic centralized in one place rather than duplicated per editor component, mirroring how `DimensionEditor` already only does trivial input-level filtering while `TreeNode` owns the real validation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [Generic Fallback Token Editor](docs/specs-archive/202608012104-generic-fallback-token-editor/)                                               |
| 2026-08-01 | Non-standard-`$type` detection (the tree-walk checking each node's own declared `$type` against the canonical list) lives in `apps/web-app/lib/tokens/standard-type.ts`, not `token-core`                                                                                                                                                                                                                                | Extends the existing "`token-core` never hard-codes an 'is this editable' policy" principle to this closely-related classification policy — it's app-level UI classification, not a spec-parsing fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | [Generic Fallback Token Editor](docs/specs-archive/202608012104-generic-fallback-token-editor/)                                               |
| 2026-08-02 | Color swatch preview (read-only and live-editor) renders via native CSS Color 4/5 function strings (`hsl()`, `oklch()`, `color(<space> ...)`, etc.) built directly from `colorSpace`/`components`/`alpha`, not a color-space-conversion library                                                                                                                                                                          | Modern browsers natively render all 14 DTCG colorSpaces from directly-built CSS strings, so zero new dependency is needed per the Minimal Dependencies constraint; `colorValueToCssColor` is the single shared implementation reused by both the read-only swatch and `ColorEditor`'s own live preview                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [Support for Colour (Color) Tokens](docs/specs-archive/202608021545-support-for-colour-tokens/)                                               |
| 2026-08-02 | `canEdit` (`TokenTree.tsx`, client) and the PATCH handler (`route.ts`, server) both generalize beyond dimension: any built-in standard type's value is validated against its own contract's `valueSchema` (via a new `resolveBuiltInContract` + the existing `validateTokenValue`) before being treated as editable/accepted, falling back to read-only/pass-through only when no built-in contract exists for that type | Discovered as a real regression once color-tokens' schema-having-but-previously-inert `color` type combined with `fallback-token-editor`'s type-name-only `canEdit` gate: a malformed color value crashed the page client-side, and a direct PATCH request could write one to disk unvalidated server-side. Generalizing the pattern `dimensionTokenType` already used fixes both and establishes the pattern any future built-in token type should follow on both sides of the edit path                                                                                                                                                                                                                                                                       | [Support for Colour (Color) Tokens](docs/specs-archive/202608021545-support-for-colour-tokens/)                                               |
| 2026-08-02 | `TokenEditorExtension.type` reverts from plain `string` back to the `DtcgTokenType` union, reversing the 2026-08-01 decision (row above)                                                                                                                                                                                                                                                                                 | Config-authoring DX (autocomplete/typo-catching in `.mts`) outweighs the earlier justification once weighed against real usage; `defineConfig`'s runtime `isDtcgTokenType` check remains the actual enforcement point either way, since a config author can still bypass the compile-time type                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [Configurable Color Spaces + Interactive Color Picker](docs/specs-archive/202608022222-configurable-color-spaces-interactive-color-picker/)   |
| 2026-08-02 | New generic `editorOptions`/`editorOptionsSchema` channel: `TokenEditorExtension` gains an optional `editorOptions: unknown`, `TokenTypeContract` gains an optional `editorOptionsSchema`, and `defineConfig` validates the former against the latter — only when a contract declares one — at config-load time                                                                                                          | Establishes a reusable mechanism for any future token-type package needing typed, validated per-type config, without forcing every contract to define a schema; a type with no `editorOptionsSchema` passes `editorOptions` through unchecked                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | [Configurable Color Spaces + Interactive Color Picker](docs/specs-archive/202608022222-configurable-color-spaces-interactive-color-picker/)   |
| 2026-08-02 | `colorjs.io/fn`'s tree-shakable entry point requires each color space to be explicitly `ColorSpace.register()`-ed before `parse`/`to` can resolve it; only the separate `colorjs.io/spaces` entry (pulling in the library's full ~40-space catalog) auto-registers                                                                                                                                                       | Confirmed empirically while wiring `token-type-color/src/conversion.ts`'s module-load-time registration loop; standing gotcha for any future `colorjs.io/fn` usage in this repo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | [Configurable Color Spaces + Interactive Color Picker](docs/specs-archive/202608022222-configurable-color-spaces-interactive-color-picker/)   |
| 2026-08-02 | The color picker's displayed swatch is computed fresh from the editor's current value on every render (`toSrgbHex(value)`), not synced into local state via an effect                                                                                                                                                                                                                                                    | Makes "editing a numeric/hex field updates the picker" true by construction — no stale-state class of bug possible — at the cost of a cheap conversion per render; reusable pattern for any future derived-display-from-source-of-truth UI                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | [Configurable Color Spaces + Interactive Color Picker](docs/specs-archive/202608022222-configurable-color-spaces-interactive-color-picker/)   |
| 2026-08-16 | Biome 2.5.8 has no Markdown support at all (parsing/formatting still "in progress" upstream) — `.md` removed from `formatter.includes`, and `format-staged.cjs`'s pre-commit hook explicitly excludes `.md` files before invoking Biome                                                                                                                                                                                | `**/*.md` in `formatter.includes` was a silent no-op the whole migration; worse, `biome check` hard-fails a commit whose staged files are *only* unsupported types ("no files were processed") rather than a silent no-op — caught only when committing a Markdown-only change. Markdown is simply not reformatted by anything now, unlike under Prettier; permanent tool-limitation gap, not a scoping choice. Reusable gotcha for any future Biome-version bump                                                                                                                                                                                                                                                                                             | [Migrate ESLint + Prettier to Biome](docs/specs-archive/202608161600-migrate-eslint-prettier-to-biome/)                                        |
| 2026-08-16 | A Biome GritQL plugin's `includes` globs require a leading `**/` to match nested directories (`**/packages/**`, not `packages/**`) — the bare form silently never matches for a plugin, even though it works fine for `files.includes`/`overrides.includes`                                                                                                                                                            | Confirmed by isolated testing against Biome's own docs example; cost real debugging time during the DI-rule GritQL plugin ports. Reusable gotcha for any future `.grit` plugin added to `biome.json`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | [Migrate ESLint + Prettier to Biome](docs/specs-archive/202608161600-migrate-eslint-prettier-to-biome/)                                        |
| 2026-08-16 | A single `.grit` file supports only one top-level applied pattern in Biome 2.5.8; a rule needing to match multiple call shapes (e.g. `crypto.randomUUID()`/`crypto.getRandomValues()`, one original ESLint selector) uses GritQL's `or { pattern1, pattern2 }` combinator inside one `where`/`register_diagnostic` block instead of two top-level blocks                                                              | Two separate top-level `` `pattern` where {...} `` blocks in the same file fail with "Failed to compile the Grit plugin", confirmed by isolated testing even with distinct metavariable names. Reusable pattern for any future multi-shape restricted-call rule                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [Migrate ESLint + Prettier to Biome](docs/specs-archive/202608161600-migrate-eslint-prettier-to-biome/)                                        |
| 2026-08-16 | `biome.json` sets `"vcs": {"enabled": true, "clientKind": "git", "useIgnoreFile": true}`                                                                                                                                                                                                                                                                                                                                  | Without it, Biome scans gitignored files Prettier/ESLint never needed a separate setting to skip (e.g. a Playwright-generated `test-results/.last-run.json` artifact broke `biome format .`); reusable setting for any repo adopting Biome fresh                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [Migrate ESLint + Prettier to Biome](docs/specs-archive/202608161600-migrate-eslint-prettier-to-biome/)                                        |

## API

| Method | Path                  | Description                                                                                                                                                                                                                                                                                                                                                 | Auth Required |
| ------ | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| GET    | /api/tokens           | Lists discovered token files under the configured directory, each marked valid/invalid and (for valid files) standard/non-standard; 500 on scan failure                                                                                                                                                                                                     | No            |
| GET    | /api/tokens/[...path] | Returns a single parsed token document; 400 (path traversal), 404 (not found), 422 (parse failure), or 500 (unexpected failure) on error                                                                                                                                                                                                                    | No            |
| PATCH  | /api/tokens/[...path] | Applies a batch of edits (rename/value/description) to any standard-DTCG-type token and/or group renames (name only) to the file and writes it back in one operation; 400 (invalid request/value, rename collision, non-standard type, value/description on a group, path traversal), 404 (not found), 422 (parse failure), or 500 (write failure) on error | No            |

## Environment & Configuration

| Key                                                        | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Required | Default |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------- |
| `apps/web-app/dtcg-editor.config.mts` (`tokensDir` field)  | Committed, code-authored config module (not an env var, not gitignored) calling `defineConfig({ tokensDir, extensions })`; `tokensDir` is the absolute/relative path (resolved against `process.cwd()`) to the directory of DTCG token files to serve                                                                                                                                                                                                                                                                                                                                     | Yes      | —       |
| `apps/web-app/dtcg-editor.config.mts` (`extensions` field) | Array of `{ type, editor, editorOptions? }` entries letting a user override which React component edits tokens of a given DTCG `$type`, and optionally pass that type's contract typed configuration (e.g. `token-type-color`'s `{ colorSpaces }` allow-list via `defineColorConfig`); `type` is validated against `token-core`'s canonical DTCG type list, and `editorOptions` (when present) against the matching built-in type's `editorOptionsSchema` (when it declares one), both at config-load time; merged with built-in defaults by `defineConfig`, user entries take precedence | No       | `[]`    |
