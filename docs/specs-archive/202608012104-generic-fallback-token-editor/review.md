# Code Review: Support for Colour (Color) Tokens

## Summary

Clean, well-scoped implementation that mirrors `token-type-dimension`'s package shape exactly, correctly stops short of making color tokens editable (per the deliberate sequencing decision deferring that to `fallback-token-editor`), and gets the DTCG 2025.10 Color module's per-colorSpace ranges right — verified directly against the live spec page during this review, not just trusted from `plan.md`. `pnpm build`, `pnpm lint`, `pnpm format:check`, and `pnpm test` (28 `token-core` + 36 `token-type-color` + 131 `web-app` unit/component/a11y tests, plus 4 Playwright e2e specs) all pass locally. A live dev-server spot check of `sample_data/color_scale.tokens.json` confirms the swatch renders correctly for `srgb`, `hsl`, `oklch`, `display-p3`, a `"none"` component, and a legacy hex value, and that an out-of-range `hsl` token still renders with its issue visibly displayed in red — AC-04/AC-05 hold up under real rendering, not just unit assertions. The Minor finding (generic "Invalid input" Zod union error for structurally-malformed color values) and the Info finding (AC-06's test not asserting the out-of-range fixture) have both been fixed and re-verified. Ready to merge.

## Findings

### 🔴 Critical

| Done | Location | Category | Problem | Suggestion |
| ---- | -------- | -------- | ------- | ---------- |

(none)

### 🟠 Major

| Done | Location | Category | Problem | Suggestion |
| ---- | -------- | -------- | ------- | ---------- |

(none)

### 🟡 Minor

| Done | Location                                                                                         | Category            | Problem                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Suggestion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---- | ------------------------------------------------------------------------------------------------ | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x]  | `apps/web-app/lib/tokens/color-display.ts:18-24`, `packages/token-type-color/src/color.ts:58-61` | UX / error messages | `ColorValueSchema` is a `z.union([ColorObjectValueSchema, LegacyHexColorValueSchema])`, and Zod v4's default union error collapses every structural failure (bad `colorSpace` enum, wrong `components` length/type, missing fields, entirely wrong shape) to the single generic message `"Invalid input"` — confirmed empirically (`ColorValueSchema.safeParse({colorSpace:"cmyk",components:[1,2,3]})` and three other malformed cases all produced `["Invalid input"]"`). This directly undercuts the feature's own user story ("I want a color token whose `$value` doesn't match its declared `colorSpace`'s shape... flagged as having an issue" implies a diagnostic message, not a content-free one) and is a real regression vs. `dimensionTokenType.valueSchema` (a plain object schema, not a union), whose Zod messages are field-specific. | **Fixed** — `describeColorForDisplay` now branches on `raw`'s JS type first and validates against `ColorObjectValueSchema`/`LegacyHexColorValueSchema` directly instead of the union, so Zod's real per-field messages surface (e.g. `"colorSpace: Invalid option: expected one of ..."`, `"components: Too small: expected array to have >=3 items"`); a malformed hex string gets a dedicated message. New tests assert the field-specific prefixes; `pnpm build`/`lint`/`format:check`/`test` re-verified green. |

### 🔵 Info / Suggestions

| Done | Location                                             | Category             | Problem                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Suggestion                                                                                                                                                                                                                                                                                              |
| ---- | ---------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x]  | `packages/token-core/src/color-sample.test.ts:12-47` | Test coverage        | The AC-06 round-trip test asserts colorSpace coverage, the `"none"` component, and the legacy-hex value, but never asserts the sample file also contains a deliberately out-of-range/malformed value (FR-07's 4th coverage point) — the file does contain one (`invalid-hue`, hue `400`), so this is a test-completeness gap, not a functional bug: nothing would catch that fixture being accidentally removed.                                                                                  | **Fixed** — added an inline hue-range check (`token-core` can't depend on `@dtcg-editor/token-type-color`'s own range table, so this uses only the universal "hue wraps at 360" fact, scoped to the four hue-bearing spaces) asserting at least one token has an out-of-range hue; re-verified passing. |
| [ ]  | `apps/web-app/lib/token-editors/built-in.ts:38-42`   | Plan/sequencing note | `TokenEditorExtension` entries here still use the pre-`fallback-token-editor` `{ filter, editor }` shape — expected and correct for this worktree in isolation (that feature's `{ type, editor }` shape change lands in a separate, currently-unmerged worktree), but `plan.md`'s own Risks section already flags this as a predictable merge-conflict point with that sibling feature. No action needed now; flagging so whoever merges second resolves it deliberately rather than by accident. | No action needed in this PR; resolve at merge time per `plan.md`'s own risk note.                                                                                                                                                                                                                       |

## Acceptance Criteria Coverage

| AC                                                                                          | Test                                                                                                                                                                                                                | Status     |
| ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| AC-01: `colorTokenType` exports `type === "color"` with correct contract shape              | `apps/web-app/lib/token-editors/built-in.test.ts`                                                                                                                                                                   | ✅ Covered |
| AC-02: `ColorValueSchema` accepts all 14 spaces + legacy bare-hex                           | `packages/token-type-color/src/color.test.ts`                                                                                                                                                                       | ✅ Covered |
| AC-03: `checkColorValueIssues` correct for in-range, out-of-range, `"none"`, and legacy-hex | `packages/token-type-color/src/color.test.ts`                                                                                                                                                                       | ✅ Covered |
| AC-04: Web app renders an accurate swatch across representative spaces + legacy hex         | `apps/web-app/lib/tokens/color-display.test.ts` + live dev-server spot check performed during this review (srgb, hsl, oklch, display-p3, `"none"`, legacy hex all render correctly)                                 | ✅ Covered |
| AC-05: Malformed color value still renders, with issues visible, no crash                   | `apps/web-app/components/TokenTree.test.tsx` ("an out-of-range color token still renders, with its issue visibly displayed (AC-05)") + live spot check (red `role="alert"` issue text rendered under `invalid-hue`) | ✅ Covered |
| AC-06: `sample_data/color_scale.tokens.json` parses and has required coverage               | `packages/token-core/src/color-sample.test.ts`, now including an assertion that the out-of-range fixture is present                                                                                                 | ✅ Covered |
| AC-07: No `token-core`/`canEdit`/`route.ts` changes; color stays non-editable               | `git diff main...HEAD` confirms zero changes to `packages/token-core`, `route.ts`, or `TokenTree.tsx`'s `canEdit` computation; existing `route.test.ts` "non-dimension token" test passes unmodified                | ✅ Covered |
| AC-08: Existing dimension-editing tests/behavior unaffected                                 | Full `pnpm test` run — 167 tests pass, zero dimension-path test files modified                                                                                                                                      | ✅ Covered |

## Post-merge addendum (rebase onto `main` with `fallback-token-editor`)

After this review, `worktree-color-tokens` was rebased onto local `main`, which by then included the merged `fallback-token-editor` feature. That feature generalized `TokenTree.tsx`'s `canEdit` gate for non-dimension standard types to check only the type _name_ (`isDtcgTokenType`), not the value's structural validity — safe for `dimension` (which already had its own value-validity guard) but not for `color`, which this feature had left structurally-checked-but-previously-inert. Combined, the two features produced two live regressions, confirmed via a running dev server:

1. A structurally malformed color value (e.g. an unrecognized `colorSpace`) crashed the whole page — `ObjectColorEditor` isn't written to handle invalid input, and the root `error.tsx` boundary is page-wide, not per-token.
2. A structurally valid but out-of-range color value (e.g. `hsl` hue `400`) became silently editable with no indication of the range violation — the previous read-only + `checkColorValueIssues` warning path was bypassed entirely.

Fixed by generalizing the dimension type's existing pattern: `apps/web-app/lib/token-editors/built-in.ts` gained `resolveBuiltInContract(type)`, and `TokenTree.tsx`'s `canEdit` now validates a non-dimension standard type's value against its built-in contract's `valueSchema` (via `validateTokenValue`) before allowing edit — a token that fails schema validation falls back to the existing read-only path instead of rendering a possibly-crashing editor. Since `ColorValueSchema` only checks shape, not per-colorSpace numeric ranges, `checkColorValueIssues`' range violations are additionally surfaced as an inline `role="alert"` list next to the now-editable color editor, so the out-of-range case is editable but never silently so. `TokenTree.test.tsx`'s AC-05 test was updated to assert the new editable+inline-warning shape. Re-verified: `pnpm build`/`lint`/`format:check`/`test` all pass (150 web-app unit/component tests + 4 e2e specs), plus live dev-server screenshots confirming both the out-of-range-hue token (editable, warning visible) and a throwaway structurally-malformed fixture (safe read-only fallback, no crash, field-specific diagnostic messages, page never crashes).

## Verdict

- [x] ✅ Ready to merge
- [ ] 🟡 Merge after minor fixes (no re-review needed)
- [ ] 🟠 Requires fixes and re-review
- [ ] 🔴 Do not merge — significant issues found
